import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.devlouix.codeeditor',
  appName: 'DL Code Editor',
  webDir: 'public'
};

export default config;
